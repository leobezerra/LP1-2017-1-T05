#include <iostream>
#include <sstream>
#include <vector>
#include <unordered_set>

#include "portforward.h"

int main (int argc, char * argv[]) {
    PortForward portfwd;
    std::cin >> portfwd;
    std::cout << portfwd;
    return 1;
}
